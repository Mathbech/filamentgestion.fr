{% extends 'base.html.twig' %}
{% block title %}Stocks{% endblock %}


{% block body%}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Stocks disponibles</h4>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th> Utilisateur </th>
                        <th> poids </th>
                        <th> Couleur </th>
                        <th> Catégorie </th>
                        <th> Prix </th>
                        <th>Date d'ajout</th>
                    </tr>
                </thead>
                <tbody>
                    {% for stock in stocks %}
                    <tr>
                        <td class="py-1">
                            {{stock.utilisateur.username }}
                        </td>
                        <td> {{ stock.poids }} </td>
                        <td>
                            {{ stock.couleur.nom }}
                        </td>
                        <td> 
                            {{stock.categorie.nomType }}
                        </td>
                        <td> {{stock.prix }} </td>
                        <td> {{stock.dateAjout|date('d/m/Y-H:m') }} </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}