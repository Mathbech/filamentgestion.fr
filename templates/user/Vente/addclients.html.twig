{% extends 'base.html.twig' %}
{% block title %}Créer un client{% endblock %}


{% block body%}

<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
                <h4 class="card-title">{{ 'client.add'|trans }}</h4>
                {# <p class="card-description"> Ajoutez vos nouveaux clients </p> #}
            <div class="forms-sample">
                {{ form_start(form) }}
                {{ form_errors(form) }}
                <div class="form-group">
                    {{ form_row(form.nom) }}
                    </div>
                <div class="form-group">
                    {{ form_row(form.prenom)}}
                </div>
                <input type="submit" class="btn btn-primary mr-2" value="Envoyer" />
                {{ form_end(form) }}
                </div>
        </div>
    </div>
</div>

{% endblock %}